%tr
  %td= platform.id
  %td= platform.name
  %td= platform.client
  %td= platform.audience
  %td
    = link_to 'Show', platform_path(platform)
    = link_to '| Edit', edit_platform_path(platform)
    = link_to '| Delete', platform_path(platform), method: :delete, data: { confirm: 'Are you sure?' }